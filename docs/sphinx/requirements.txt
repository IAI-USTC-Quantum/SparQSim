# Sphinx documentation build requirements
sphinx>=7.0
furo
myst-parser
sphinx-copybutton
sphinx-autoapi
nbsphinx
ipykernel
nbconvert>=7.0.0
# pysparq 纯 Python 层的 autodoc 导入依赖 numpy（编译扩展 _core 仍不可用时，
# 至少 rir/operators/algorithms 等纯 Python 模块可正常渲染）
numpy
# C++ API 集成：渲染 docs/doxygen 生成的 XML（doxygen 本体需单独安装，CI 中由 apt 提供）
breathe
