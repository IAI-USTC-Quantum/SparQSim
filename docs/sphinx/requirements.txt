# Sphinx documentation build requirements
sphinx>=7.0
furo
myst-parser
sphinx-copybutton
sphinx-autoapi
nbsphinx
ipykernel
nbconvert>=7.0.0
# The pure-Python layer of pysparq relies on numpy for autodoc imports (even when the
# compiled extension _core is unavailable, pure-Python modules such as rir/operators/algorithms can still render)
numpy
# C++ API integration: render the XML generated under docs/doxygen (Doxygen itself must be installed separately; provided by apt in CI)
breathe
