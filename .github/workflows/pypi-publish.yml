name: Publish to PyPI

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  # Gitea 也会扫描 .github/workflows,但其 runner 访问不了 github.com,
  # 且发布只应在 GitHub 上游仓库发生 → owner 不符时全部跳过
  build_wheels:
    name: Build wheel (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    if: github.repository_owner == 'IAI-USTC-Quantum'
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      # submodules: true 拉取 extern/qram-simulator（相对 URL 在 GitHub
      # 上解析到本 org 的 QRAM-Simulator 仓库）；fetch-depth: 0 供
      # setuptools-scm 读版本
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install CMake
        uses: jwlawson/actions-setup-cmake@v2
        with:
          cmake-version: '3.26.x'

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Build wheels
        uses: pypa/cibuildwheel@v2.22
        env:
          CIBW_BUILD: "cp310-* cp311-* cp312-* cp313-*"
          CIBW_SKIP: "*-musllinux_* *-win32 *-macos*"

      - uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}
          path: ./wheelhouse/*.whl

  build_sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    if: github.repository_owner == 'IAI-USTC-Quantum'
    steps:
      # checkout 需初始化 submodule:pyproject 的 sdist.include 会把
      # extern/qram-simulator 内容打进 sdist（PyPI 要求 sdist 自包含）
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Build sdist
        run: |
          pip install build setuptools-scm
          python -m build --sdist

      - name: Verify sdist is self-contained
        run: |
          tar -tzf dist/pysparq-*.tar.gz | grep -q "extern/qram-simulator/SparQ/include/sparse_state_simulator.h" \
            || { echo "ERROR: sdist missing embedded QRAM-Simulator core"; exit 1; }

      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*.tar.gz

  publish:
    name: Publish to PyPI
    needs: [build_wheels, build_sdist]
    runs-on: ubuntu-latest
    if: github.repository_owner == 'IAI-USTC-Quantum' && (github.event_name == 'push' || github.event_name == 'release')
    environment:
      name: pypi
      url: https://pypi.org/project/pysparq/

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist/
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
